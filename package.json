{
  "name": "review-harvest",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "clean": "turbo clean && yarn rimraf node_modules"
  },
  "devDependencies": {
    "@tooling/eslint-config": "*",
    "@tooling/typescript-config": "*",
    "prettier": "^3.2.5",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*",
    "tooling/*",
    "internal/*"
  ],
  "dependencies": {
    "rimraf": "^5.0.5"
  }
}
